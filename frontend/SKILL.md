---
name: frontend
description: Redirects to frontend-ux — the complete guide for handling encrypted state in your dApp UI.
---

# Frontend — Encrypted State in Your UI

**This skill has been merged into `frontend-ux/SKILL.md`**, which covers everything here plus:
- The three decryption types (public decrypt, user decrypt, delegate decrypt)
- Signature caching to prevent repeated wallet popups
- XSS prevention for cached signatures
- Key management for production
- Complete button state machine (6 states)

**Fetch `frontend-ux/SKILL.md` instead.**
